{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}


    <h1>Première page Sortir</h1>


    {% for event in events %}
        <article>
            <a href="{{ path('serie_show', {'id': serie.id}) }}">
                <img src="{{ asset('img/posters/series/' ~ serie.poster) }}" alt="{{ serie.name }}" title="{{ serie.name }} | {{ serie.seasons | length }} seasons"></a>
        </article>
    {% endfor %}

{% endblock %}
