{% extends 'base.html.twig' %}


{% block title %}Créer une sortie{% endblock %}



{% block body %}


    <h1>TODO = Ceci est la page sur laquelle il faut créer un formulaire pour créer une sortie </h1>
<div class="container">
    {{ form_start(eventForm) }}
    {{ form_widget(eventForm) }}
    {{ form_errors(eventForm) }}

    <button class="login-button green">Créer</button>
    {{ form_end(eventForm) }}
    </div>
{% endblock %}
